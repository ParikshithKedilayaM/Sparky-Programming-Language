begin
	var x = 5;
	var xyz = 10;
	var abcdef;
	var y = true;
	display(x+5);
	if(x+y==y+z) 
		then x++; 
	else y++; 
	endif;
	for(i:=1;i<10;i++)
		do if(x+y==y+z) 
			then x++; 
		else y++; 
		endif;
	endfor;
	while(x==y)
		do if(x+y==y+z) 
			then x++; 
		else y++; 
		endif;
	endwhile;
	if (true)
		then 
		while(x==y)
			do if(x+y==y+z) 
				then x++; 
			else y++; 
			endif;
		endwhile;
	endif;
	display((x+y)*(x-y)/(x+x+x+x*x*x*x-x-x-x-x));
end.
